#include<stdio.h>
// find greTER NUMBER  using Ternary operator.
void main()
{
//	int no=10;
//	no%2==0?printf("even"): printf("odd");
	
	//    ? = condition ,  : = else ,  ; =stop .this are three ternary operators.
	int a=100,b=120,c=30;
	a>b ? a>c ? printf("A is greater") : printf("C is greater") : b>c ? printf("B is greater") : printf("C is greater") ;
	
	
}
